import type { AgentEvent } from "../index";

const sessionId = "fixture-coding-session";

export const codingSessionFixture: readonly AgentEvent[] = [
  { type: "session.start", sessionId },
  {
    type: "user.message",
    sessionId,
    messageId: "user-1",
    content: "Add request rate limiting to the API, cover it with a test, and make sure the workspace stays clean.",
  },
  {
    type: "assistant.message.delta",
    sessionId,
    messageId: "assistant-1",
    delta: "I’ll trace the server shape first, then make the smallest tested change.\n\n",
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "read-1",
    toolName: "realm/read",
    input: { path: "src/server.ts" },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "read-1",
    status: "succeeded",
    output: {
      lineCount: 18,
      content: "import express from \"express\";\n\nconst app = express();\napp.use(express.json());\n\napp.get(\"/health\", (_req, res) => {\n  res.json({ status: \"ok\" });\n});\n\nexport { app };",
    },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "search-1",
    toolName: "mcp__workspace__search",
    input: { query: "supertest OR request(app)", path: "test" },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "search-1",
    status: "succeeded",
    output: {
      results: [
        { file: "test/health.test.ts", line: 3, preview: "import request from \"supertest\";" },
        { file: "test/health.test.ts", line: 8, preview: "const response = await request(app).get(\"/health\");" },
        { file: "package.json", line: 22, preview: "\"supertest\": \"^7.0.0\"" },
      ],
    },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "todo-1",
    toolName: "todo",
    input: {
      items: [
        { text: "Inspect server and tests", status: "done" },
        { text: "Add rate-limit middleware", status: "pending" },
        { text: "Cover the 429 response", status: "pending" },
      ],
    },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "todo-1",
    status: "succeeded",
    output: {
      items: [
        { text: "Inspect server and tests", status: "done" },
        { text: "Add rate-limit middleware", status: "pending" },
        { text: "Cover the 429 response", status: "pending" },
      ],
    },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "lsp-1",
    toolName: "MCP__typescript__LSP",
    input: { operation: "references", symbol: "app", path: "src/server.ts" },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "lsp-1",
    status: "succeeded",
    output: { result: "4 references across src/server.ts and 2 test files" },
  },
  {
    type: "assistant.message.delta",
    sessionId,
    messageId: "assistant-1",
    delta: "The app export is already testable. I’ll add a local middleware with no new runtime dependency.\n\n",
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "edit-1",
    toolName: "edit",
    input: {
      path: "src/server.ts",
      diff: "@@ -2,6 +2,13 @@\n const app = express();\n+const requests = new Map<string, number>();\n+app.use((req, res, next) => {\n+  const count = (requests.get(req.ip) ?? 0) + 1;\n+  requests.set(req.ip, count);\n+  if (count > 100) return res.status(429).json({ error: \"rate_limited\" });\n+  next();\n+});\n app.use(express.json());",
    },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "edit-1",
    status: "succeeded",
    output: { summary: "Updated src/server.ts", added: 7, removed: 0 },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "write-1",
    toolName: "realm/write",
    input: {
      path: "test/rate-limit.test.ts",
      language: "ts",
      content: "import { expect, test } from \"bun:test\";\nimport request from \"supertest\";\nimport { app } from \"../src/server\";\n\ntest(\"limits repeated requests\", async () => {\n  for (let index = 0; index < 100; index += 1) {\n    await request(app).get(\"/health\");\n  }\n  const response = await request(app).get(\"/health\");\n  expect(response.status).toBe(429);\n});",
    },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "write-1",
    status: "succeeded",
    output: { path: "test/rate-limit.test.ts", bytes: 412 },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "bash-1",
    toolName: "mcp__shell__bash",
    input: { command: "bun test && bun run typecheck" },
    status: "running",
  },
  {
    type: "tool_call.update",
    sessionId,
    toolCallId: "bash-1",
    status: "running",
    output: { stdout: "bun test v1.2.\n✓ health endpoint\n" },
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "bash-1",
    status: "succeeded",
    output: { stdout: "✓ rate limit\n2 pass, 0 fail\nTypecheck clean", exitCode: 0 },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "task-1",
    toolName: "task",
    input: { role: "reviewer", task: "Review the rate-limit change for edge cases" },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "task-1",
    status: "succeeded",
    output: { status: "complete", summary: "Implementation is scoped and tested. Reset state between tests before production use." },
  },
  {
    type: "tool_call.start",
    sessionId,
    toolCallId: "custom-1",
    toolName: "mcp__preview__publish_snapshot",
    input: { label: "rate-limit-review", visibility: "team" },
    status: "running",
  },
  {
    type: "tool_call.end",
    sessionId,
    toolCallId: "custom-1",
    status: "succeeded",
    output: { url: "preview://rate-limit-review", expiresIn: "2h" },
  },
  {
    type: "assistant.message.delta",
    sessionId,
    messageId: "assistant-1",
    delta: "Done. The API now returns `429` after the configured request budget, the behavior is covered by a focused test, and the workspace passes both checks.\n\n```ts\nif (count > 100) {\n  return res.status(429).json({ error: \"rate_limited\" });\n}\n```",
  },
  { type: "session.done", sessionId },
];
