export interface MarketplaceFilter<T extends string = string> { readonly id: T; readonly label: string; readonly title?: string }
export function MarketplaceFilterPills<T extends string>({ filters, active, onChange, disabled = false }: { readonly filters: readonly MarketplaceFilter<T>[]; readonly active: T; readonly onChange: (id: T) => void; readonly disabled?: boolean }) { return <div className="fraym-filter-pills" role="radiogroup">{filters.map(option => <button aria-checked={active === option.id} className={active === option.id ? "is-active" : undefined} disabled={disabled} key={option.id} onClick={() => onChange(option.id)} role="radio" title={option.title} type="button">{option.label}</button>)}</div>; }
