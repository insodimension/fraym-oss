import type { MouseEventHandler, ReactNode } from "react";
import { classNames } from "../elements/utils";
export interface CollapsibleProps { readonly title: ReactNode; readonly open: boolean; readonly onToggle: () => void; readonly leading?: ReactNode; readonly count?: ReactNode; readonly actions?: ReactNode; readonly onContextMenu?: MouseEventHandler<HTMLButtonElement>; readonly children: ReactNode; readonly className?: string; readonly triggerClassName?: string; readonly contentClassName?: string }
export function Collapsible({ title, open, onToggle, leading, count, actions, onContextMenu, children, className, triggerClassName, contentClassName }: CollapsibleProps) { return <div className={classNames("fraym-collapsible", className)} data-slot="collapsible" data-state={open ? "open" : "closed"}><div className="fraym-collapsible__header" data-slot="collapsible-header"><button aria-expanded={open} className={classNames("fraym-collapsible__trigger", triggerClassName)} data-slot="collapsible-trigger" onClick={onToggle} onContextMenu={onContextMenu} type="button"><span>{leading}<span>{title}</span></span><span>{count === undefined ? null : <span className="fraym-collapsible__count">{count}</span>}<span aria-hidden="true" className="fraym-collapsible__caret" data-slot="collapsible-caret">›</span></span></button>{actions ? <div className="fraym-collapsible__actions" data-slot="collapsible-actions">{actions}</div> : null}</div>{open ? <div className={contentClassName} data-slot="collapsible-content">{children}</div> : null}</div>; }
