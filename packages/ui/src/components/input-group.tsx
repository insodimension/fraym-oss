import type { ComponentProps, ReactNode } from "react";
import { classNames } from "../elements/utils";
export interface InputGroupProps extends Omit<ComponentProps<"input">, "size"> { readonly leading?: ReactNode; readonly trailing?: ReactNode; readonly inputClassName?: string; readonly variant?: "default" | "ghost"; readonly size?: "sm" | "default" }
export function InputGroup({ leading, trailing, variant = "default", size = "default", className, inputClassName, ...props }: InputGroupProps) { return <div className={classNames("fraym-input-group", `fraym-input-group--${variant}`, `fraym-input-group--${size}`, className)} data-size={size} data-slot="input-group" data-variant={variant}>{leading != null ? <span data-slot="input-group-leading">{leading}</span> : null}<input className={classNames("fraym-input-group__input", inputClassName)} data-slot="input-group-input" {...props} />{trailing != null ? <span data-slot="input-group-trailing">{trailing}</span> : null}</div>; }
