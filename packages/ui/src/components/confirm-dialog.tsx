import type { ReactNode } from "react";
import { Button } from "../elements/Button";
import { Modal } from "../elements/Popover";
export type ConfirmDialogIntent = "default" | "danger";
export interface ConfirmDialogProps { readonly title: string; readonly description?: ReactNode; readonly details?: ReactNode; readonly confirmLabel?: string; readonly cancelLabel?: string; readonly intent?: ConfirmDialogIntent; readonly icon?: string; readonly busy?: boolean; readonly onConfirm: () => void; readonly onClose: () => void }
export function ConfirmDialog({ title, description, details, confirmLabel = "Confirm", cancelLabel = "Cancel", intent = "default", icon, busy = false, onConfirm, onClose }: ConfirmDialogProps) { return <Modal aria-label={title} className="fraym-confirm-dialog" onClose={onClose} role="alertdialog"><div data-slot="confirm-dialog"><div className={`fraym-confirm-dialog__icon is-${intent}`} aria-hidden="true">{icon ?? (intent === "danger" ? "×" : "✓")}</div><div className="fraym-confirm-dialog__copy"><strong>{title}</strong>{description ? <div>{description}</div> : null}</div>{details ? <div className="fraym-confirm-dialog__details">{details}</div> : null}<div className="fraym-confirm-dialog__actions"><Button disabled={busy} onClick={onClose} variant="outline">{cancelLabel}</Button><Button disabled={busy} onClick={onConfirm} variant={intent === "danger" ? "danger" : "primary"}>{confirmLabel}</Button></div></div></Modal>; }
