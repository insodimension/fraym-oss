export interface AgentSetupProps { readonly pluginName: string; readonly requirementLabel: string }
export function AgentSetup({ pluginName, requirementLabel }: AgentSetupProps) { return <div className="fraym-fix-hint" data-slot="plugin-agent-hint"><span aria-hidden="true" className="fraym-fix-hint__icon">✦</span><span><strong>{requirementLabel} needs a short conversation</strong><small>Opens a setup session for {pluginName} and walks through the remaining steps.</small></span></div>; }
export function agentSetupBrief(pluginId: string, pluginName: string, requirementLabel: string, detail?: string) { return [`Help me finish setting up the "${pluginName}" plugin.`, "", `Unmet requirement: ${requirementLabel}${detail ? ` — ${detail}` : ""}`, "", `Find the "${pluginId}" plugin's setup documentation and walk me through completing this requirement.`, "Ask me for anything you need rather than assuming, then tell me how to verify the plugin is ready."].join("\n"); }
