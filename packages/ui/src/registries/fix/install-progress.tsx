import { Spinner } from "../../elements/Spinner";
export interface InstallProgressProps { readonly command: string; readonly installing: boolean; readonly error?: string | null }
export function InstallProgress({ command, installing, error }: InstallProgressProps) { if (error) return <div className="fraym-fix-error" data-slot="plugin-install-error">{error}</div>; return <div className="fraym-fix-hint" data-slot={installing ? "plugin-install-progress" : "plugin-install-hint"}>{installing ? <Spinner label={`Installing ${command}`} size="sm" /> : <span aria-hidden="true" className="fraym-fix-hint__icon">›_</span>}<span><strong>{installing ? `Installing ${command}…` : `Requires the ${command} CLI`}</strong><small>{installing ? "This can take a minute." : "Install it once so the connector can use it."}</small></span></div>; }
