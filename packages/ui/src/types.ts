export type Size = "sm" | "default" | "lg" | "icon";

/** Badge/label tone names. `accent|add|blue|warn|mute` mirror the prototype's
 * `.mtag` classes exactly; `del` is a library addition backed by `--fr-del`. */
export type Tone = "accent" | "add" | "blue" | "warn" | "mute" | "del";
