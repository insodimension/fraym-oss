import { useEffect, useState } from "react"; import { classNames } from "../../elements/utils";
export const COMPOSER_TIPS: readonly string[] = ["Type / to browse commands and skills.", "Shift+Enter adds a newline; Enter sends.", "Drop files or images directly into the composer.", "Click the context ring to inspect the active window.", "Choose a permission mode before sensitive work."];
export interface ComposerTipsProps { readonly tips?: readonly string[]; readonly intervalMs?: number; readonly className?: string }
export function ComposerTips({ tips = COMPOSER_TIPS, intervalMs = 16_000, className }: ComposerTipsProps) { const [start] = useState(() => tips.length ? Math.floor(Math.random() * tips.length) : 0); const [step, setStep] = useState(0); useEffect(() => { if (tips.length <= 1 || intervalMs <= 0) return; const timer = setInterval(() => setStep((value) => value + 1), intervalMs); return () => clearInterval(timer); }, [intervalMs, tips.length]); if (!tips.length) return null; return <div className={classNames("fraym-composer-tip", className)} data-slot="composer-tip"><strong>Tip:</strong> {tips[(start + step) % tips.length]}</div>; }
