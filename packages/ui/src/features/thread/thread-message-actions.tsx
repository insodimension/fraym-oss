import { useMemo } from "react"; import type { MessageBlock, MessageData } from "../message"; import type { MessageAction } from "../../elements/message-actions";
export function messagePlainText(blocks: readonly MessageBlock[]): string { return blocks.flatMap((block) => { const text = typeof block.text === "string" ? block.text : typeof block.message === "string" ? block.message : ""; return text ? [text] : []; }).join("\n\n"); }
export interface ThreadMessageActions { readonly actions: readonly MessageAction[]; readonly plainText: string }
export function useThreadMessageActions(message: MessageData): ThreadMessageActions { const plainText = messagePlainText(message.blocks); return useMemo(() => ({ plainText, actions: plainText ? [{ id: "copy", label: "Copy message", icon: "copy", onClick: () => void navigator.clipboard?.writeText(plainText) }] : [] }), [plainText]); }
