import type { HostUiRequest, HostUiResponse } from "@fraym/driver"; import type { ReactNode } from "react"; import { resolveSurfaceRegistration, type SurfaceRendererMap } from "../../registries/surface-renderer-registry"; import { HostUiDialog, HostUiRequestSurface } from "./host-ui-dialog";
export interface HostUiLayerProps { readonly requests: readonly HostUiRequest[]; readonly onRespond: (response: HostUiResponse) => void; readonly renderers?: SurfaceRendererMap }
export function HostUiLayer({ requests, onRespond, renderers = {} }: HostUiLayerProps) { const request = requests[0]; if (!request) return null; const key = `hostUi:${request.kind}`; const custom = renderers[key]; if (custom) { const registration = resolveSurfaceRegistration(renderers, key); return <>{registration.render({ channel: "hostUi", request }, { respond: onRespond })}</>; } return <HostUiDialog onRespond={onRespond} request={request} />; }
export function ConnectedHostUiLayer(): ReactNode { return null; }
export function HostUiDock({ requests, onRespond, renderers = {} }: HostUiLayerProps): ReactNode { return <div className="fraym-host-ui-dock">{requests.map((request) => { const key = `hostUi:${request.kind}`; const registration = renderers[key] ? resolveSurfaceRegistration(renderers, key) : null; return <div key={request.requestId ?? request.id}>{registration ? registration.render({ channel: "hostUi", request }, { respond: onRespond }) : <HostUiRequestSurface onRespond={onRespond} request={request} />}</div>; })}</div>; }
export function ConnectedSelectDialog(): ReactNode { return null; }
