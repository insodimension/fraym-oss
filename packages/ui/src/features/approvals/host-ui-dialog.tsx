import type { HostUiRequest, HostUiResponse, PermissionOption } from "@fraym/driver"; import { Modal } from "../../elements/popover"; import { FallbackSurface } from "./fallback-surface"; import { PermissionApprovalCard, type PermissionHostUiRequest } from "./permission-approval-card"; import { ConfirmRequestPicker, InputRequestCard, SelectRequestPicker, type ConfirmHostUiRequest, type InputHostUiRequest, type SelectHostUiRequest } from "./select-request-picker";
export type DialogHostUiRequest = HostUiRequest;
export interface HostUiDialogProps { readonly request: DialogHostUiRequest; readonly onRespond: (response: HostUiResponse) => void; readonly className?: string }
function permission(request: HostUiRequest): request is PermissionHostUiRequest { return request.kind === "permission" && typeof request.title === "string" && typeof request.toolName === "string" && Array.isArray(request.options) && request.options.every((option) => typeof option === "object" && option !== null && "optionId" in option); }
export function HostUiRequestSurface({ request, onRespond }: { readonly request: HostUiRequest; readonly onRespond: (response: HostUiResponse) => void }) { if (request.kind === "select" && request.title && Array.isArray(request.options)) return <SelectRequestPicker onRespond={onRespond} request={request as SelectHostUiRequest} />; if (request.kind === "input" && request.title) return <InputRequestCard onRespond={onRespond} request={request as InputHostUiRequest} />; if (request.kind === "confirm" && request.title) return <ConfirmRequestPicker onRespond={onRespond} request={request as ConfirmHostUiRequest} />; if (permission(request)) return <PermissionApprovalCard onRespond={onRespond} request={{ ...request, options: request.options as readonly PermissionOption[] }} />; return <FallbackSurface ctx={{ respond: onRespond }} input={{ channel: "hostUi", request }} />; }
export function HostUiDialog({ request, onRespond, className }: HostUiDialogProps) { const close = () => onRespond({ requestId: request.requestId ?? request.id, cancelled: true }); return <Modal aria-label={request.title ?? "Request"} className={`fraym-host-ui-dialog${className ? ` ${className}` : ""}`} onClose={close}><HostUiRequestSurface onRespond={onRespond} request={request} /></Modal>; }
