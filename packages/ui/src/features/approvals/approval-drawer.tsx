import type { HostUiRequest, HostUiResponse } from "@fraym/driver"; import { BottomSheet } from "../../components/bottom-sheet"; import { HostUiRequestSurface } from "./host-ui-dialog";
export type ApprovalDrawerRequest = HostUiRequest;
export interface ApprovalDrawerProps { readonly request: ApprovalDrawerRequest; readonly onRespond: (response: HostUiResponse) => void; readonly className?: string }
export function ApprovalDrawer({ request, onRespond, className }: ApprovalDrawerProps) { const requestId = request.requestId ?? request.id; return <BottomSheet ariaLabel={request.title ?? "Approval request"} {...(className ? { className } : {})} onClose={() => onRespond({ requestId, cancelled: true })} title={request.title ?? "Request"}><HostUiRequestSurface onRespond={onRespond} request={request} /></BottomSheet>; }
