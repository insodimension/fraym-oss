{
	"name": "@fraym-ai/driver-codex",
	"version": "0.1.0",
	"type": "module",
	"description": "Fraym SessionDriver backed by the local Codex CLI bridge",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"files": ["src", "bridge.ts", "!src/**/*.test.ts", "!src/**/*.test.tsx"],
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"bridge": "bun bridge.ts"
	},
	"dependencies": {
		"@fraym-ai/driver": "workspace:*"
	}
}
