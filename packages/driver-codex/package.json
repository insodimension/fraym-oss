{
	"name": "@fraym/driver-codex",
	"version": "0.0.0",
	"type": "module",
	"description": "Fraym SessionDriver backed by the local Codex CLI bridge",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"files": ["src", "bridge.ts"],
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"bridge": "bun bridge.ts"
	},
	"dependencies": {
		"@fraym/driver": "workspace:*"
	}
}
