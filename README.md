# Fraym

Fraym is an open source React UI kit for coding agents, providing first-class agent surfaces such as streaming threads, tool-call renderers, approvals, and composers behind a typed driver contract, with deterministic replay and live Agent Client Protocol adapters that let any agent harness plug in.
